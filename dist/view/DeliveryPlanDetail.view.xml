<mvc:View
    controllerName="myapp.controller.DeliveryPlanDetail"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form">
    <Page title="{i18n>deliveryPlanListTitle}" showNavButton="true" navButtonPress="onNavBack">
        <content>
            <VBox class="detailForm">
                <f:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12" adjustLabelSpan="false" emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="1" columnsL="1" columnsM="1" singleContainerFullSize="false">
                    <f:content>
                        <Label text="{i18n>deliveryId}" />
                        <Input value="{deliveryPlans>id}" editable="false" />
                        <Label text="{i18n>deliveryDate}" />
                        <Input value="{deliveryPlans>date}" />
                        <Label text="{i18n>deliveryStatus}" />
                        <Input value="{deliveryPlans>status}" />
                        <Label text="{i18n>poId}" />
                        <Link text="{deliveryPlans>purchaseOrderId}" press="onNavigateToPurchaseOrder" />
                    </f:content>
                </f:SimpleForm>
                <HBox justifyContent="End" class="sapUiSmallMarginTop">
                    <Button text="{i18n>saveButton}" press="onSave" type="Emphasized" />
                    <Button text="{i18n>cancelButton}" press="onCancel" />
                </HBox>
            </VBox>
        </content>
    </Page>
</mvc:View>